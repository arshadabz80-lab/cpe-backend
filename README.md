# CPE Backend (Supabase + Render)

### Endpoints:
GET / → Health check  
POST /add-competitor  
GET /competitors  

### Deploy on Render:
- Upload repo  
- Auto-install works  
- Uses process.env.PORT  
